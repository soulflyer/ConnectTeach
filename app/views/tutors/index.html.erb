<h1>Listing tutors</h1>

<table>
  <tr>
	  <th></th>
		<th>Login</th>
		<th>Admin</th>
    <th>Name</th>
    <th>Age</th>
    <th>Sex</th>
    <th>Flavour</th>
		<% if session[:tutor_id] && Tutor.find(session[:tutor_id]).admin %>
			<th></th>
		<% end %>
  </tr>

<% @tutors.each do |tutor| %>
  <tr>
	  <td><%=link_to (image_tag (tutor.detail.photo.url(:thumb))), tutor%></td>
		<td><%=h tutor.email %></td>
		<td><%=h tutor.admin %></td>
    <td></td>
     <td><%=h tutor.age %></td>
    <td><%=h tutor.sex %></td>
    <td><%=h tutor.flavour %></td>
		<% if session[:tutor_id] && Tutor.find(session[:tutor_id]).admin %>
			<td><%= link_to 'Show', tutor %>
				<%= link_to 'Edit', edit_tutor_path(tutor) %>
				<%= link_to 'Destroy', tutor, :confirm => 'Are you sure?', :method => :delete %>
			</td>
		<% end %>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New tutor', new_tutor_path %>