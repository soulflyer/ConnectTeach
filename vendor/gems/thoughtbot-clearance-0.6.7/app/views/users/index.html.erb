<h1>Listing users</h1>

<table>
  <tr>
    <th>EMail</th>
		<th>Confirmed</th>
		<th>Details</th>
		<th>Role</th>
		<th></th>
    <th></th>
  </tr>

<% @user.each do |user| %>
  <tr>
    <td><%=h user.email %></td>
		<td><%=h user.email_confirmed %></td>
		<td><%=h user.tutor_id %></td>
		<td><%=h user.role %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
		<td><%= link_to 'Edit', edit_user_path(user) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>